# Required in production
DATABASE_URL=
BLOB_READ_WRITE_TOKEN=
ADMIN_PASSWORD=
AUTH_SECRET=

# Optional local fallback while DATABASE_URL/BLOB_READ_WRITE_TOKEN are missing
NEXT_PUBLIC_APP_URL=http://localhost:3000
